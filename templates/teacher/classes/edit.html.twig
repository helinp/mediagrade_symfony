{% extends 'base.html.twig' %}

{% block page_title %}Modifier une classe
{% endblock %}

{% block body %}
	<div class="row">
		<div class="col-xs-12 col mb-2">
			<div class="box">
				<div class="box-body">

					{{ include('teacher/classes/_form.html.twig', {'button_label': 'Update'}) }}
				</div>
				<div class="box-footer">
					<a href="{{ path('classes_index') }}">Retourner à la liste</a>

					{{ include('teacher/classes/_delete_form.html.twig') }}
				</div>
			</div>
		{% endblock %}
